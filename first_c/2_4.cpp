#include <iostream>
using namespace std;
int main() {
	int n;
	// 输入 n， 表示有N组测试数据，
	cin >> n;
	// 后是对应的测试数据，每行为一个整数。
	int a[n];
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}
	
	// 循环每一个输入的整数
	for (int i = 0; i < n; i++) {
		var temp = a[i];
		while (temp % 2 == )
		// 求其对应2进制数中1的个数
	}
	

	
}

